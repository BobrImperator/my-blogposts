# We're on Vite

<img src="/snapshot/fresh_welcome_page_in_react.png" />

Note:

We're on Vite! The image is showcasing a default Ember app built as a library and embedded into a React page.



@TODO: -- OBSOLETE --
Why twice? For anyone who's ever tried to naively build and embedded an Ember app might've ran into a problem loading an Ember app into a page that already has an AMD loader in the context.
I only want to make a note of that and not go into details. These days are gone, Vite support goes as far as Ember 3.28 and Vite build is when you'd want to make your app embeddable.

Going back to the app, there's something missing, where are the assets?
